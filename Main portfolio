<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Hrishikesh Patil — Data Analyst</title>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,300;0,400;0,500;0,700;1,400&family=Share+Tech+Mono&display=swap" rel="stylesheet">
<style>
  :root {
    --green: #00ff88;
    --green-dim: #00cc6a;
    --green-glow: rgba(0,255,136,0.15);
    --green-faint: rgba(0,255,136,0.06);
    --amber: #ffb800;
    --cyan: #00d4ff;
    --bg: #080c09;
    --bg2: #0d1410;
    --bg3: #111a14;
    --surface: #141f17;
    --surface2: #1a2a1d;
    --border: rgba(0,255,136,0.12);
    --border-strong: rgba(0,255,136,0.28);
    --text: #c8d8cc;
    --text-dim: #6a8a72;
    --text-faint: #3a5040;
    --red: #ff4444;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    line-height: 1.7;
    overflow-x: hidden;
  }

  /* Scanline overlay */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0,0,0,0.03) 2px,
      rgba(0,0,0,0.03) 4px
    );
    pointer-events: none;
    z-index: 1000;
  }

  /* Matrix rain canvas */
  #matrix-canvas {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    opacity: 0.03;
    pointer-events: none;
    z-index: 0;
  }

  /* Nav */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    background: rgba(8,12,9,0.92);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border);
    padding: 0 2rem;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo {
    color: var(--green);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.05em;
  }

  .nav-logo span { color: var(--text-dim); font-weight: 400; }

  .nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
  }

  .nav-links a {
    color: var(--text-dim);
    text-decoration: none;
    font-size: 12px;
    transition: color 0.2s;
    letter-spacing: 0.05em;
  }

  .nav-links a::before { content: '~/'; color: var(--text-faint); }
  .nav-links a:hover { color: var(--green); }

  .nav-status {
    display: flex;
    align-items: center;
    gap: 7px;
    font-size: 11px;
    color: var(--text-dim);
  }

  .status-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 6px var(--green);
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 6px var(--green); }
    50% { opacity: 0.5; box-shadow: 0 0 2px var(--green); }
  }

  /* Sections */
  section { position: relative; z-index: 1; }

  /* Hero */
  #hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 100px 0 60px;
    background: radial-gradient(ellipse 60% 50% at 50% 40%, rgba(0,255,136,0.04) 0%, transparent 70%);
  }

  .hero-inner {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
    width: 100%;
  }

  .terminal-window {
    background: var(--bg2);
    border: 1px solid var(--border-strong);
    border-radius: 8px;
    box-shadow: 0 0 60px rgba(0,255,136,0.08), 0 0 120px rgba(0,255,136,0.04);
    overflow: hidden;
  }

  .terminal-bar {
    background: var(--surface);
    padding: 10px 14px;
    display: flex;
    align-items: center;
    gap: 8px;
    border-bottom: 1px solid var(--border);
  }

  .dot { width: 11px; height: 11px; border-radius: 50%; }
  .dot-r { background: #ff5f57; }
  .dot-y { background: #febc2e; }
  .dot-g { background: #28c840; }

  .terminal-title {
    margin-left: 8px;
    font-size: 11px;
    color: var(--text-dim);
    flex: 1;
    text-align: center;
    letter-spacing: 0.05em;
  }

  .terminal-body {
    padding: 28px 28px 36px;
    min-height: 340px;
  }

  .prompt-line {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin-bottom: 6px;
  }

  .prompt-sym {
    color: var(--green);
    user-select: none;
    flex-shrink: 0;
    padding-top: 2px;
  }

  .cmd { color: var(--text); }
  .output { color: var(--text-dim); margin-left: 20px; margin-bottom: 12px; }
  .output .highlight { color: var(--green); }
  .output .amber { color: var(--amber); }
  .output .cyan { color: var(--cyan); }

  #typing-target { color: var(--green); font-size: 15px; font-weight: 500; }
  #typing-target .cursor {
    display: inline-block;
    width: 9px; height: 16px;
    background: var(--green);
    vertical-align: text-bottom;
    animation: blink 0.9s step-end infinite;
    margin-left: 2px;
  }

  @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 20px 0 0 20px;
  }

  .tag {
    font-size: 11px;
    padding: 3px 10px;
    border: 1px solid var(--border-strong);
    border-radius: 3px;
    color: var(--green-dim);
    background: var(--green-faint);
    letter-spacing: 0.04em;
  }

  .hero-cta {
    display: flex;
    gap: 14px;
    margin: 28px 0 0 20px;
    flex-wrap: wrap;
  }

  .btn {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    padding: 9px 20px;
    border-radius: 4px;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.2s;
    letter-spacing: 0.05em;
  }

  .btn-primary {
    background: var(--green);
    color: #080c09;
    border: 1px solid var(--green);
    font-weight: 700;
  }

  .btn-primary:hover {
    background: transparent;
    color: var(--green);
    box-shadow: 0 0 16px rgba(0,255,136,0.3);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text-dim);
    border: 1px solid var(--border-strong);
  }

  .btn-secondary:hover { color: var(--green); border-color: var(--green); }

  /* Section shared */
  .section-wrap {
    max-width: 900px;
    margin: 0 auto;
    padding: 80px 2rem;
  }

  .section-header {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-bottom: 40px;
  }

  .section-header .sh-prompt { color: var(--green); font-size: 13px; }

  .section-title {
    font-size: 22px;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.02em;
  }

  .section-header::after {
    content: '';
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, var(--border-strong), transparent);
  }

  /* Stats bar */
  #stats {
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .stats-grid {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    divide: 1px solid var(--border);
  }

  .stat-item {
    padding: 28px 20px;
    border-right: 1px solid var(--border);
    text-align: center;
  }

  .stat-item:last-child { border-right: none; }

  .stat-num {
    font-size: 32px;
    font-weight: 700;
    color: var(--green);
    display: block;
    line-height: 1;
    margin-bottom: 6px;
    font-family: 'Share Tech Mono', monospace;
  }

  .stat-label {
    font-size: 11px;
    color: var(--text-dim);
    letter-spacing: 0.05em;
  }

  /* Experience */
  .exp-entry {
    position: relative;
    padding-left: 28px;
    margin-bottom: 40px;
  }

  .exp-entry::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    bottom: -40px;
    width: 1px;
    background: linear-gradient(180deg, var(--border-strong), transparent);
  }

  .exp-entry:last-child::before { display: none; }

  .exp-dot {
    position: absolute;
    left: -5px;
    top: 7px;
    width: 11px;
    height: 11px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 10px rgba(0,255,136,0.5);
  }

  .exp-meta {
    display: flex;
    align-items: baseline;
    gap: 10px;
    margin-bottom: 4px;
    flex-wrap: wrap;
  }

  .exp-role {
    font-size: 15px;
    font-weight: 700;
    color: var(--text);
  }

  .exp-company {
    font-size: 13px;
    color: var(--green-dim);
  }

  .exp-period {
    font-size: 11px;
    color: var(--text-faint);
    margin-left: auto;
    white-space: nowrap;
  }

  .exp-bullets {
    list-style: none;
    margin-top: 12px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .exp-bullets li {
    color: var(--text-dim);
    font-size: 13px;
    padding-left: 20px;
    position: relative;
    line-height: 1.6;
  }

  .exp-bullets li::before {
    content: '>';
    position: absolute;
    left: 0;
    color: var(--green);
    font-weight: 700;
  }

  .exp-bullets li .metric {
    color: var(--amber);
    font-weight: 600;
  }

  /* Skills */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 14px;
  }

  .skill-block {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 18px 20px;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .skill-block:hover {
    border-color: var(--border-strong);
    box-shadow: 0 0 20px rgba(0,255,136,0.05);
  }

  .skill-block-title {
    font-size: 11px;
    color: var(--green);
    letter-spacing: 0.1em;
    margin-bottom: 12px;
    font-weight: 700;
  }

  .skill-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .skill-tag {
    font-size: 11px;
    padding: 3px 9px;
    background: var(--green-faint);
    border: 1px solid var(--border);
    border-radius: 3px;
    color: var(--text-dim);
  }

  .skill-bar-wrap {
    margin-top: 14px;
  }

  .skill-bar-item {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 8px;
  }

  .skill-bar-label {
    font-size: 11px;
    color: var(--text-dim);
    width: 90px;
    flex-shrink: 0;
  }

  .skill-bar-track {
    flex: 1;
    height: 3px;
    background: var(--surface2);
    border-radius: 2px;
    overflow: hidden;
  }

  .skill-bar-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--green-dim), var(--green));
    border-radius: 2px;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 1s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .skill-bar-fill.animated { transform: scaleX(1); }

  .skill-bar-pct {
    font-size: 10px;
    color: var(--text-faint);
    width: 28px;
    text-align: right;
  }

  /* Education & Certs */
  .edu-card {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 22px 24px;
    margin-bottom: 14px;
    display: flex;
    gap: 20px;
    align-items: flex-start;
    transition: border-color 0.2s;
  }

  .edu-card:hover { border-color: var(--border-strong); }

  .edu-icon {
    width: 40px; height: 40px;
    border: 1px solid var(--border-strong);
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 16px;
    flex-shrink: 0;
    background: var(--green-faint);
  }

  .edu-degree { font-size: 14px; font-weight: 600; color: var(--text); margin-bottom: 3px; }
  .edu-school { font-size: 12px; color: var(--green-dim); margin-bottom: 3px; }
  .edu-meta { font-size: 11px; color: var(--text-faint); }

  .certs-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    margin-top: 14px;
  }

  .cert-card {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 16px;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .cert-card:hover {
    border-color: var(--border-strong);
    box-shadow: 0 0 16px rgba(0,255,136,0.05);
  }

  .cert-badge {
    font-size: 9px;
    letter-spacing: 0.1em;
    color: var(--amber);
    background: rgba(255,184,0,0.08);
    border: 1px solid rgba(255,184,0,0.2);
    padding: 2px 7px;
    border-radius: 2px;
    display: inline-block;
    margin-bottom: 8px;
    font-weight: 700;
  }

  .cert-name { font-size: 12px; color: var(--text); font-weight: 500; line-height: 1.4; }

  /* Contact */
  #contact { background: var(--bg2); border-top: 1px solid var(--border); }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    align-items: start;
  }

  .contact-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 16px;
    border: 1px solid var(--border);
    border-radius: 6px;
    margin-bottom: 10px;
    transition: border-color 0.2s;
    text-decoration: none;
  }

  .contact-item:hover { border-color: var(--green); }

  .contact-item-icon {
    font-size: 18px;
    width: 36px;
    text-align: center;
    flex-shrink: 0;
  }

  .contact-item-label { font-size: 10px; color: var(--text-faint); display: block; }
  .contact-item-val { font-size: 12px; color: var(--text); }

  .contact-msg {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 20px;
    font-size: 13px;
    color: var(--text-dim);
    line-height: 1.8;
  }

  .contact-msg .green { color: var(--green); }

  /* Footer */
  footer {
    border-top: 1px solid var(--border);
    padding: 20px 2rem;
    text-align: center;
    font-size: 11px;
    color: var(--text-faint);
  }

  footer span { color: var(--green-dim); }

  /* Reveal animation */
  .reveal {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Scrollbar */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--text-faint); border-radius: 3px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--text-dim); }

  @media (max-width: 680px) {
    .stats-grid { grid-template-columns: repeat(2, 1fr); }
    .skills-grid { grid-template-columns: 1fr; }
    .certs-grid { grid-template-columns: 1fr; }
    .contact-grid { grid-template-columns: 1fr; }
    .nav-links { display: none; }
  }
</style>
</head>
<body>

<canvas id="matrix-canvas"></canvas>

<!-- NAV -->
<nav>
  <div class="nav-logo">hrishi@portfolio<span>:~$</span></div>
  <ul class="nav-links">
    <li><a href="#experience">experience</a></li>
    <li><a href="#skills">skills</a></li>
    <li><a href="#education">education</a></li>
    <li><a href="#contact">contact</a></li>
  </ul>
  <div class="nav-status">
    <div class="status-dot"></div>
    available for work
  </div>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-inner">
    <div class="terminal-window">
      <div class="terminal-bar">
        <div class="dot dot-r"></div>
        <div class="dot dot-y"></div>
        <div class="dot dot-g"></div>
        <div class="terminal-title">hrishikesh@portfolio — zsh — 120×38</div>
      </div>
      <div class="terminal-body">
        <div class="prompt-line">
          <span class="prompt-sym">❯</span>
          <span class="cmd">whoami</span>
        </div>
        <div class="output" id="output-whoami" style="opacity:0;">
          <span class="highlight">Hrishikesh Patil</span> — Data Analyst · Mumbai, IN<br>
          <span class="amber">1.6 yrs</span> turning messy business data into decisions that stick
        </div>

        <div class="prompt-line" id="prompt-2" style="opacity:0;">
          <span class="prompt-sym">❯</span>
          <span class="cmd">cat ./summary.txt</span>
        </div>
        <div class="output" id="output-summary" style="opacity:0;">
          Proficient in <span class="cyan">SQL · Python · Power BI</span> across FMCG + logistics.<br>
          Built dashboards that cut reporting time by <span class="amber">50%</span>.<br>
          Reduced logistics costs by <span class="amber">6%</span> through network optimization.<br>
          <span class="highlight">Azure AZ-900 + AI-900</span> certified.
        </div>

        <div class="prompt-line" id="prompt-3" style="opacity:0;">
          <span class="prompt-sym">❯</span>
          <span id="typing-target"><span class="cursor"></span></span>
        </div>

        <div class="hero-tags" id="hero-tags" style="opacity:0;">
          <span class="tag">SQL</span>
          <span class="tag">Python</span>
          <span class="tag">Power BI</span>
          <span class="tag">DAX</span>
          <span class="tag">pandas</span>
          <span class="tag">Azure</span>
          <span class="tag">ETL</span>
          <span class="tag">SAP</span>
          <span class="tag">Tableau</span>
          <span class="tag">scikit-learn</span>
        </div>

        <div class="hero-cta" id="hero-cta" style="opacity:0;">
          <a href="#contact" class="btn btn-primary">./contact.sh</a>
          <a href="#experience" class="btn btn-secondary">cat experience.log</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- STATS -->
<section id="stats">
  <div class="stats-grid">
    <div class="stat-item reveal">
      <span class="stat-num" data-target="50">0</span>
      <div class="stat-label">% reporting time saved</div>
    </div>
    <div class="stat-item reveal">
      <span class="stat-num" data-target="4">0</span>
      <div class="stat-label">% RTV/RTO cost reduced</div>
    </div>
    <div class="stat-item reveal">
      <span class="stat-num" data-target="6">0</span>
      <div class="stat-label">% logistics cost cut</div>
    </div>
    <div class="stat-item reveal">
      <span class="stat-num" data-target="95">0</span>
      <div class="stat-label">% ML model accuracy</div>
    </div>
  </div>
</section>

<!-- EXPERIENCE -->
<section id="experience">
  <div class="section-wrap">
    <div class="section-header">
      <span class="sh-prompt">$</span>
      <h2 class="section-title">experience</h2>
    </div>

    <div class="exp-entry reveal">
      <div class="exp-dot"></div>
      <div class="exp-meta">
        <span class="exp-role">Data Analyst</span>
        <span class="exp-company">@ The Whole Truth Foods</span>
        <span class="exp-period">Sep 2024 → present</span>
      </div>
      <ul class="exp-bullets">
        <li>Analyzed sales, margin, and GTM data across regional accounts — identified SKU gaps and anomalies, driving a <span class="metric">4% reduction</span> in RTV/RTO costs.</li>
        <li>Built automated Power BI dashboards with DAX for ops, finance, and sales teams — cut reporting time by <span class="metric">50%</span> with real-time stakeholder visibility.</li>
        <li>Used <span class="metric">Python (pandas, NumPy)</span> to clean, transform, and automate pipelines from SAP, GT software, and D2C platforms.</li>
        <li>Led SFA implementation end-to-end — master data setup in SAP, SOP documentation, and workflow automation across ops, finance, and sales.</li>
      </ul>
    </div>

    <div class="exp-entry reveal">
      <div class="exp-dot"></div>
      <div class="exp-meta">
        <span class="exp-role">Associate Data Analyst</span>
        <span class="exp-company">@ LogisticsNow</span>
        <span class="exp-period">Jan 2024 → Sep 2024</span>
      </div>
      <ul class="exp-bullets">
        <li>Analyzed freight and dispatch data using SQL and Python — contributed to a <span class="metric">6% reduction</span> in logistics costs through network optimization.</li>
        <li>Built dashboards in Tableau and Looker Studio to track route efficiency and fleet KPIs — findings improved dispatch performance by <span class="metric">15%</span>.</li>
        <li>Supported ETL workflows for internal reporting tools; gathered requirements from procurement and planning heads; tracked delivery in <span class="metric">Jira</span>.</li>
      </ul>
    </div>

    <div class="exp-entry reveal">
      <div class="exp-dot"></div>
      <div class="exp-meta">
        <span class="exp-role">Project Intern</span>
        <span class="exp-company">@ E-Sparse Matrix Solutions</span>
        <span class="exp-period">Jan 2022 → Jun 2023</span>
      </div>
      <ul class="exp-bullets">
        <li>Applied machine learning (scikit-learn) on eye-tracking datasets — data acquisition, preprocessing, and model training achieving <span class="metric">95% prediction accuracy</span>.</li>
      </ul>
    </div>
  </div>
</section>

<!-- SKILLS -->
<section id="skills" style="background: var(--bg2); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border);">
  <div class="section-wrap">
    <div class="section-header">
      <span class="sh-prompt">$</span>
      <h2 class="section-title">skills --verbose</h2>
    </div>

    <div class="skills-grid">
      <div class="skill-block reveal">
        <div class="skill-block-title">// QUERY LANGUAGES</div>
        <div class="skill-bar-wrap">
          <div class="skill-bar-item">
            <span class="skill-bar-label">SQL (MySQL)</span>
            <div class="skill-bar-track"><div class="skill-bar-fill" style="--w:0.92"></div></div>
            <span class="skill-bar-pct">92%</span>
          </div>
          <div class="skill-bar-item">
            <span class="skill-bar-label">PostgreSQL</span>
            <div class="skill-bar-track"><div class="skill-bar-fill" style="--w:0.85"></div></div>
            <span class="skill-bar-pct">85%</span>
          </div>
          <div class="skill-bar-item">
            <span class="skill-bar-label">DAX</span>
            <div class="skill-bar-track"><div class="skill-bar-fill" style="--w:0.80"></div></div>
            <span class="skill-bar-pct">80%</span>
          </div>
        </div>
      </div>

      <div class="skill-block reveal">
        <div class="skill-block-title">// PYTHON STACK</div>
        <div class="skill-bar-wrap">
          <div class="skill-bar-item">
            <span class="skill-bar-label">pandas</span>
            <div class="skill-bar-track"><div class="skill-bar-fill"></div></div>
            <span class="skill-bar-pct">90%</span>
          </div>
          <div class="skill-bar-item">
            <span class="skill-bar-label">NumPy</span>
            <div class="skill-bar-track"><div class="skill-bar-fill"></div></div>
            <span class="skill-bar-pct">85%</span>
          </div>
          <div class="skill-bar-item">
            <span class="skill-bar-label">scikit-learn</span>
            <div class="skill-bar-track"><div class="skill-bar-fill"></div></div>
            <span class="skill-bar-pct">75%</span>
          </div>
        </div>
      </div>

      <div class="skill-block reveal">
        <div class="skill-block-title">// VISUALIZATION</div>
        <div class="skill-tags">
          <span class="skill-tag">Power BI</span>
          <span class="skill-tag">Tableau</span>
          <span class="skill-tag">Looker Studio</span>
          <span class="skill-tag">matplotlib</span>
          <span class="skill-tag">Power Query</span>
        </div>
      </div>

      <div class="skill-block reveal">
        <div class="skill-block-title">// AUTOMATION & ETL</div>
        <div class="skill-tags">
          <span class="skill-tag">Power Automate</span>
          <span class="skill-tag">UIPath</span>
          <span class="skill-tag">VBA</span>
          <span class="skill-tag">Pipeline Design</span>
          <span class="skill-tag">SAP Integration</span>
        </div>
      </div>

      <div class="skill-block reveal">
        <div class="skill-block-title">// CLOUD PLATFORMS</div>
        <div class="skill-tags">
          <span class="skill-tag">Microsoft Azure</span>
          <span class="skill-tag">Azure SQL</span>
          <span class="skill-tag">Azure AI Fundamentals</span>
          <span class="skill-tag">AZ-900 Certified</span>
        </div>
      </div>

      <div class="skill-block reveal">
        <div class="skill-block-title">// TOOLS & PRACTICES</div>
        <div class="skill-tags">
          <span class="skill-tag">Jira</span>
          <span class="skill-tag">Confluence</span>
          <span class="skill-tag">Advanced Excel</span>
          <span class="skill-tag">Data Governance</span>
          <span class="skill-tag">KPI Tracking</span>
          <span class="skill-tag">R</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- EDUCATION -->
<section id="education">
  <div class="section-wrap">
    <div class="section-header">
      <span class="sh-prompt">$</span>
      <h2 class="section-title">education</h2>
    </div>

    <div class="edu-card reveal">
      <div class="edu-icon">🎓</div>
      <div>
        <div class="edu-degree">B.E. — Electronics & Computer Science</div>
        <div class="edu-school">Mumbai University</div>
        <div class="edu-meta">May 2024 &nbsp;·&nbsp; GPA: 8.39/10 &nbsp;·&nbsp; Honors: Blockchain Technology</div>
        <div class="edu-meta" style="margin-top:4px;">Coursework: Data Analysis · Machine Learning · Algorithms · Artificial Intelligence</div>
      </div>
    </div>

    <div class="section-header" style="margin-top: 40px;">
      <span class="sh-prompt">$</span>
      <h2 class="section-title" style="font-size: 18px;">certifications</h2>
    </div>

    <div class="certs-grid">
      <div class="cert-card reveal">
        <div class="cert-badge">MICROSOFT</div>
        <div class="cert-name">Azure Fundamentals (AZ-900)</div>
      </div>
      <div class="cert-card reveal">
        <div class="cert-badge">MICROSOFT</div>
        <div class="cert-name">Azure AI Fundamentals (AI-900)</div>
      </div>
      <div class="cert-card reveal">
        <div class="cert-badge">ICT ACADEMY</div>
        <div class="cert-name">Data Analytics</div>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="section-wrap">
    <div class="section-header">
      <span class="sh-prompt">$</span>
      <h2 class="section-title">contact</h2>
    </div>

    <div class="contact-grid">
      <div>
        <a href="mailto:hrishikeshpatil1404@gmail.com" class="contact-item">
          <div class="contact-item-icon">📧</div>
          <div>
            <span class="contact-item-label">EMAIL</span>
            <span class="contact-item-val">hrishikeshpatil1404@gmail.com</span>
          </div>
        </a>
        <a href="tel:+917900177274" class="contact-item">
          <div class="contact-item-icon">📞</div>
          <div>
            <span class="contact-item-label">PHONE</span>
            <span class="contact-item-val">+91 7900177274</span>
          </div>
        </a>
        <div class="contact-item" style="cursor:default;">
          <div class="contact-item-icon">📍</div>
          <div>
            <span class="contact-item-label">LOCATION</span>
            <span class="contact-item-val">Mumbai, Maharashtra, IN</span>
          </div>
        </div>
      </div>
      <div class="contact-msg">
        <span class="green">// open to opportunities</span><br><br>
        Currently at The Whole Truth Foods, but always open to conversations about interesting data problems — especially in FMCG, analytics infrastructure, or anything that involves making messy data actually useful.<br><br>
        <span class="green">$</span> ping me anytime.
      </div>
    </div>
  </div>
</section>

<footer>
  built by <span>Hrishikesh Patil</span> &nbsp;·&nbsp; Mumbai &nbsp;·&nbsp; <span>2025</span>
</footer>

<script>
// Matrix rain
(function() {
  const canvas = document.getElementById('matrix-canvas');
  const ctx = canvas.getContext('2d');
  let cols, drops;

  function init() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    const fontSize = 13;
    cols = Math.floor(canvas.width / fontSize);
    drops = Array(cols).fill(1);
  }

  function draw() {
    ctx.fillStyle = 'rgba(8,12,9,0.05)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = '#00ff88';
    ctx.font = '13px Share Tech Mono';
    const chars = '01アイウエオカキクケコ∑∏√∞≈≠ABCDEF0123456789';
    drops.forEach((y, i) => {
      const char = chars[Math.floor(Math.random() * chars.length)];
      ctx.fillText(char, i * 13, y * 13);
      if (y * 13 > canvas.height && Math.random() > 0.975) drops[i] = 0;
      drops[i]++;
    });
  }

  init();
  window.addEventListener('resize', init);
  setInterval(draw, 60);
})();

// Terminal typing sequence
(function() {
  const lines = [
    { delay: 600, fn: () => fade('output-whoami') },
    { delay: 1400, fn: () => fade('prompt-2') },
    { delay: 2000, fn: () => fade('output-summary') },
    { delay: 2800, fn: () => {
      fade('prompt-3');
      setTimeout(() => typeText('ls ./skills', () => {
        setTimeout(() => {
          fade('hero-tags');
          setTimeout(() => fade('hero-cta'), 300);
        }, 300);
      }), 200);
    }}
  ];

  function fade(id) {
    const el = document.getElementById(id);
    if (el) el.style.transition = 'opacity 0.4s', el.style.opacity = '1';
  }

  function typeText(text, cb) {
    const target = document.getElementById('typing-target');
    let i = 0;
    const cursor = '<span class="cursor"></span>';
    const interval = setInterval(() => {
      if (i < text.length) {
        target.innerHTML = text.slice(0, ++i) + cursor;
      } else {
        clearInterval(interval);
        if (cb) cb();
      }
    }, 65);
  }

  lines.forEach(({ delay, fn }) => setTimeout(fn, delay));
})();

// Skill bar animation on scroll
(function() {
  const bars = document.querySelectorAll('.skill-bar-fill');
  const widths = [0.92, 0.85, 0.80, 0.90, 0.85, 0.75];
  bars.forEach((bar, i) => {
    bar.style.width = (widths[i] || 0.80) * 100 + '%';
    bar.style.transform = 'scaleX(0)';
  });

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.querySelectorAll('.skill-bar-fill').forEach(b => b.classList.add('animated'));
      }
    });
  }, { threshold: 0.3 });

  document.querySelectorAll('.skill-block').forEach(b => observer.observe(b));
})();

// Reveal on scroll
(function() {
  const obs = new IntersectionObserver((entries) => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 80);
        obs.unobserve(e.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.reveal').forEach(el => obs.observe(el));
})();

// Stat counter animation
(function() {
  const nums = document.querySelectorAll('.stat-num[data-target]');
  const obs = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const el = entry.target;
        const target = parseInt(el.dataset.target);
        let current = 0;
        const step = Math.ceil(target / 30);
        const interval = setInterval(() => {
          current = Math.min(current + step, target);
          el.textContent = current + '%';
          if (current >= target) clearInterval(interval);
        }, 40);
        obs.unobserve(el);
      }
    });
  }, { threshold: 0.5 });
  nums.forEach(n => obs.observe(n));
})();
</script>
</body>
</html>
